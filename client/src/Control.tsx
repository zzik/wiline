import React from "react";

// container component for switching between different database functionalities
const Control = () => {
  return <section className="control">Control</section>;
};

export default Control;
